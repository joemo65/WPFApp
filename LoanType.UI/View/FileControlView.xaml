<UserControl x:Class="LoanType.UI.View.FileControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LoanType.UI.View"
             mc:Ignorable="d" d:DesignWidth="605" Height="58">
    <Grid>
        <Label x:Name="FileLabel" Content="File:" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" RenderTransformOrigin="0.5,0"/>
        <TextBox x:Name="FileTextBox" HorizontalAlignment="Left" Height="23" Margin="46,14,0,0" TextWrapping="Wrap" Text="{Binding FileName}" VerticalAlignment="Top" Width="316" RenderTransformOrigin="0.5,0" ClipToBounds="True"/>
        <Button x:Name="BrowseButton" Content="Browse" HorizontalAlignment="Left" Margin="367,16,0,0" VerticalAlignment="Top" Width="64" Command="{Binding BrowseCommand}" />
        <Button x:Name="ImportButton" Content="Import" HorizontalAlignment="Left" Margin="436,16,0,0" VerticalAlignment="Top" Width="63" RenderTransformOrigin="0.5,0" Command="{Binding ImportCommand}"/>
        <Button x:Name="ExportButton" Content="Export" HorizontalAlignment="Left" Margin="504,16,0,0" VerticalAlignment="Top" Width="75" Command="{Binding ExportCommand}"/>
    </Grid>
</UserControl>
